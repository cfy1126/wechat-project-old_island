<view class="container"> 
    <image bind:tap="onLeft" src="{{latest?disLeftSrc:leftSrc}}" class="icon" />
    <text class="title">{{title}}</text>
    <image bind:tap="onRight" src="{{first?disRightSrc:rightSrc}}" class="icon" />
</view>